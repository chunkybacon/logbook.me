= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => {:class => 'form'}) do |f|
  = devise_error_messages!
  #box
    #block-signup.block
      %h2 User Details
      .content
        %p
          = f.label :email, :class => 'label'
          = f.text_field :email, :class => 'text_field'
        %p
          = f.label :password, 'Password (6 chars min)', :class => 'label'
          = f.password_field :password, :class => 'text_field'
        %p
          = f.label :password_confirmation, :class => 'label'
          = f.password_field :password_confirmation, :class => 'text_field'
  #box
    .block
      %h2 Application Details
      .content
        - resource.applications.build if resource.applications.empty?
        = f.fields_for :applications do |af|
          %p
            = af.label :name, "Your application name", :class => 'label'
            = af.text_field :name, :class => 'text_field'
          %p
            = af.label :time_zone, "and time zone", :class => 'label'
            = af.time_zone_select :time_zone, nil, :class => 'select_field'
        .group.navform.wat-cf  
          %button.button{:type => 'submit'}
            =image_tag('web-app-theme/icons/tick.png', :alt => 'Sign in')
            Sign up

