#block-text.block
  .content
    .inner
      %h2.title You've got no log entries yet. Here's how you can get started

      = render :partial => 'shared/setup_instructions', :locals => { :application => @application }
